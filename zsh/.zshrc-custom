# Add user bin directory from cabal and pip
PATH=${PATH}:${HOME}/.cabal/bin
PATH=${PATH}:${HOME}/.local/bin

# General aliases
# alias ll='ls -l'
# alias la='ls -a'
# alias lla='ls -la'
# alias df='df --human-readable'
# alias du='du --human-readable'
# alias s="cd .."
# alias cp='cp --interactive'
# alias mv='mv --interactive'
# alias rm='rm --interactive'

# System aliases
#alias sagi="sudo apt-get install"
#alias sagupd="sudo apt-get update"
#alias sagupg="sudo apt-get upgrade"
#alias spci="sudo pacman -S"
#alias spcu="sudo pacman -Syu"
alias pacro="sudo pacman -Rns $(pacman -Qtdq)"
alias aurin="sudo aura -A"
alias aurupg="sudo aura -Au"
alias yaoupg="yaourt -Sbu --aur"

alias st="subl3"
alias sst="sudo subl3"

alias khdd="cd /mnt/kevin"
alias lhdd="cd /mnt/LinuxHDD"
alias ram="cd /mnt/RAMDisk"

alias pidr="pip install -r dev-requirements.txt"
alias pipin3="pip3 install --user"
alias pipout3="pip3 list --outdated"
pipupg3() {
    list=($(pip3 list --outdated | awk '{print $1}'))
    for package in "${list[@]}"; do
        pip3 install --user --upgrade $package
    done
}
alias pipin2="pip2 install --user"
alias pipout2="pip2 list --outdated"
pipupg2() {
    list=($(pip2 list --outdated | awk '{print $1}'))
    for package in "${list[@]}"; do
        pip2 install --user --upgrade $package
    done
}

alias djrs="python manage.py runserver"
alias djt="python manage.py test"

alias nb2="ipython2 notebook"
alias nb3="ipython3 notebook"

alias zshrcc="vim ~/.zshrc-custom && source ~/.zshrc-custom"

alias gkl="livestreamer dailymotion.com/embed/video/x13533w"

# export USE_RAMFS=1
# export DB_NAME="smart_prod"
export EDITOR="vim"
export BROWSER="firefox"

# ibus exports
export GTK_IM_MODULE=ibus
export XMODIFIERS=@im=ibus
export QT_IM_MODULE=ibus
ibus-daemon -drx

# Options
#set -o clobber
#[[ -n "${key[Up]}"      ]] && bindkey  "${key[Up]}"      history-beginning-search-backward
#[[ -n "${key[Down]}"    ]] && bindkey  "${key[Down]}"    history-beginning-search-forward


function clickable() {
    PREFIX="file://$(pwd)/";
    sed "s*^*$PREFIX*" |
    sed 's/ /%20/g';
}

